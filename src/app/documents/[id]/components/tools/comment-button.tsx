import { MessageSquarePlusIcon } from 'lucide-react';

import { ToolButton } from '../toolbar-button';

const CommentButton = () => {
  const handleClick = () => {};

  return <ToolButton icon={MessageSquarePlusIcon} onClick={handleClick} />;
};

export { CommentButton };
